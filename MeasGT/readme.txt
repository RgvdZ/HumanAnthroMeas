See there is photo id which is the name of the photos in the folder, and the subject id that is being represented as the id in the other 2 files, I am training a neural network that will take 2 images, image 1 and image 2 and will do CPD algorithm on it(all images are silhouettes 1 channel).  the cpd will make img 1 and img 2 , and vcie versa. the ask that it will geenrate(eg img 1 + mask = img2) will be given to a CNN and and the CNN will predict the 14 body measrement of img 2, we habe the gt measuremrnt for both. these is how ill train my NN to get measurement of the other image. i future if i have to get a meaurment of silhoeueete(all poses are A pose in the silhouette) then ill tkae one image with know gt and then other measurmen and basd on this method will generate it igt measurements. the dataset is BodyM dataset. discuss about ths